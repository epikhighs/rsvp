import 'react';
import './App.css';

const App = ({apple}) => (
  <div className="App">
    <h1>Welcome to RSVP</h1>
    <div>{apple}</div>
    <div>{apple}</div>
    <div>{apple}</div>
  </div>
);

export default App;
